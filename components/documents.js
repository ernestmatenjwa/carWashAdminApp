import * as React from 'react';
import { Text, View } from 'react-native';

export default function DocumentsScreen({ navigation }) {
  return (
    <View>
       <Text>This is DocumentsScreen screen</Text>
    </View>
  );
}