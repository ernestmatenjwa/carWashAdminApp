import * as React from 'react';
import { Text, View } from 'react-native';

export default function InformationScreen({ navigation }) {
  return (
    <View>
       <Text>This is InformationScreen screen</Text>
    </View>
  );
}