import * as React from 'react';
import { Text, View } from 'react-native';

export default function AdminProfileScreen({ navigation }) {
  return (
    <View>
       <Text>This is adminProfile screen</Text>
    </View>
  );
}